<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Odoo 19: mail.channel became discuss.channel and the 'public'
             field was dropped. -->
        <record model="discuss.channel" id="channel_xml">
            <field name="name">xml</field>
            <field name="channel_type">channel</field>
            <field name="description">XML Channel to get informed when a XML arrive.</field>
        </record>
    </data>
</odoo>
