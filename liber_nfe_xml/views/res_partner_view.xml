<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <data>
        <!-- Inherit : FORM View-->
        <record id="inherit_res_partner_form_id" model="ir.ui.view">
            <field name="name">res.partner.form.inherit</field>
            <field name="model">res.partner</field>
            <field name="inherit_id" ref="base.view_partner_form"/>
            <field name="arch" type="xml">
                <xpath expr="//page[@name='sales_purchases']/group" position="inside">
                    <group name="vendor_xml" string="Vendor XML" groups="base.group_no_one">
                        <field name="identification_no"/>
                        <field name="po_tag"/>
                        <field name="vendor_tag"/>
                    </group>
                </xpath>
            </field>
        </record>

        <!-- LAB FORK: re-anchored to the base company form (production
             version hangs this on the edoo_br_mde company view). -->
        <record id="inherit_res_company_form_view" model="ir.ui.view">
            <field name="name">res.company.form.inherit</field>
            <field name="model">res.company</field>
            <field name="inherit_id" ref="base.view_company_form"/>
            <field name="arch" type="xml">
                <xpath expr="//field[@name='currency_id']" position="after">
                    <field name="nfe_xml_partner_id"/>
                </xpath>
            </field>
        </record>
    </data>
</odoo>